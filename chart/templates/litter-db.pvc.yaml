apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: {{ .Values.mongo.volume.pvc.name }}-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}

spec:
  accessModes:
    - ReadWriteOnce
  volumeName: {{ .Values.mongo.volume.pv.name }}
  storageClassName: {{ .Values.mongo.volume.storageClassName }}

  resources:
    requests:
      storage: {{ .Values.mongo.volume.size }}